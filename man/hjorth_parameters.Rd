% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fractal.R
\name{hjorth_parameters}
\alias{hjorth_parameters}
\title{Hjorth Mobility and Complexity}
\usage{
hjorth_parameters(x)
}
\arguments{
\item{x}{Numeric vector. The time series to analyse.}
}
\value{
A named list with \code{mobility} and \code{complexity}.
}
\description{
Computes the Hjorth parameters (Hjorth 1970): mobility, a proxy for mean
frequency derived from the variance ratio of the first difference to
the signal itself; and complexity, a proxy for bandwidth/irregularity
derived from the second difference. Ported from Lucas Franca's own
\code{mrpheus} package (AASM staging feature pipeline). Uses Bessel-corrected
(\code{ddof = 1}) variance throughout, deliberately matching R's \code{var()}
convention -- this was mrpheus's original design choice, and differs
slightly from the \code{antropy} Python library's population-variance
(\code{ddof = 0}) convention. The two converge as \code{length(x)} grows but
differ meaningfully for short series; see \code{inst/COPYRIGHTS}.
}
\examples{
set.seed(1)
hjorth_parameters(rnorm(1000))

}
\references{
Hjorth B. EEG analysis based on time domain properties.
Electroencephalogr Clin Neurophysiol 1970;29(3):306-310.
}
