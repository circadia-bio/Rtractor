% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fractal.R
\name{dfa}
\alias{dfa}
\title{Detrended Fluctuation Analysis (DFA)}
\usage{
dfa(
  x,
  order = 1,
  min_box = NULL,
  max_box = NULL,
  integrate = TRUE,
  sliding_window = FALSE
)
}
\arguments{
\item{x}{Numeric vector. The time series to analyse.}

\item{order}{Integer >= 1. Order of the polynomial detrending fit
(\code{1} = linear detrending, the DFA default; \code{2} = quadratic, etc.).
Corresponds to \code{nfit - 1} in the original \code{dfa.c}.}

\item{min_box, max_box}{Integer or \code{NULL}. Smallest/largest box size to
evaluate. Defaults (as in the original): \code{min_box = 2 * (order + 1)},
\code{max_box = length(x) / 4}.}

\item{integrate}{Logical. If \code{TRUE} (default), \code{x} is treated as an
increment series and cumulatively summed before analysis (the usual
DFA convention — e.g. pass RR-interval deviations, not a cumulative
profile). Set \code{FALSE} if \code{x} is already an integrated/cumulative
profile.}

\item{sliding_window}{Logical. Use overlapping (sliding) windows instead
of non-overlapping boxes. Default \code{FALSE}.}
}
\value{
A list with:
\item{n}{Box sizes evaluated (integer vector).}
\item{F}{RMS fluctuation at each box size (numeric vector).}
\item{alpha}{The DFA scaling exponent: the slope of \code{log10(F)} on
\code{log10(n)}.}
}
\description{
Estimates the scaling exponent alpha of a time series using Detrended
Fluctuation Analysis (Peng et al. 1994). This is a direct C++ port of
the reference \code{dfa.c} implementation distributed by PhysioNet (Mietus,
Peng & Moody), validated to reproduce the original compiled binary's
output exactly on synthetic test data — see \code{inst/COPYRIGHTS}.
}
\examples{
set.seed(1)
dfa(rnorm(2000))

}
\references{
Peng C-K, Buldyrev SV, Havlin S, Simons M, Stanley HE, Goldberger AL.
Mosaic organization of DNA nucleotides. Phys Rev E 1994;49:1685-1689.
}
