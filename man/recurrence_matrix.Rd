% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rqa.R
\name{recurrence_matrix}
\alias{recurrence_matrix}
\title{Recurrence Matrix}
\usage{
recurrence_matrix(
  x,
  m = 1L,
  tau = 1L,
  radius = NULL,
  rr = NULL,
  norm = c("euclidean", "maximum", "manhattan"),
  theiler_window = 1L
)
}
\arguments{
\item{x}{Numeric vector. The time series to analyse.}

\item{m}{Integer. Embedding dimension. Default \code{1} (no embedding --
recurrence of the raw scalar series). Use \code{\link[=estimate_embed_dim]{estimate_embed_dim()}} to
choose this for a proper phase-space reconstruction.}

\item{tau}{Integer. Time delay used if \code{m > 1}. Default \code{1}. Use
\code{\link[=estimate_delay]{estimate_delay()}} to choose this.}

\item{radius}{Numeric or \code{NULL}. Fixed vicinity threshold. Exactly one
of \code{radius}/\code{rr} should be given (or neither, see below).}

\item{rr}{Numeric in \verb{(0, 1)} or \code{NULL}. Target recurrence rate: \code{radius}
is set to the empirical quantile of pairwise distances (outside the
Theiler band) that yields this recurrence rate. If both \code{radius} and
\code{rr} are \code{NULL}, \code{rr = 0.1} is used (a common default for comparable
RQA measures across signals).}

\item{norm}{Distance norm: \code{"euclidean"} (default), \code{"maximum"}
(Chebyshev), or \code{"manhattan"}.}

\item{theiler_window}{Integer. Points within this many samples of the
main diagonal are excluded from \code{rr}/distance-quantile calculations
(they are trivially close in time, not dynamically informative).
Default \code{1} (excludes just the main diagonal \code{i == j}).}
}
\value{
A list with:
\item{matrix}{Logical \verb{N x N} recurrence matrix.}
\item{distance}{Numeric \verb{N x N} distance matrix (before thresholding).}
\item{radius}{The threshold used.}
\item{rr}{The achieved recurrence rate (Theiler band excluded).}
\item{m, tau, theiler_window, norm}{Echoed parameters, for
\code{\link[=rqa_measures]{rqa_measures()}} and downstream use.}

This is O(N^2) in both time and memory (a dense \verb{N x N} matrix); a
warning is issued above 5000 points.
}
\description{
Builds a binary recurrence matrix from a time series: \code{R[i, j] = 1} if
the (optionally embedded) trajectory points \code{i} and \code{j} are within a
distance \code{radius} of each other, \code{0} otherwise (Eckmann, Kamphorst &
Ruelle 1987). No reference implementation was specified for this port
(no preference given) -- implemented directly from the standard
formulas in Marwan et al. (2007). See \code{inst/COPYRIGHTS}.
}
\examples{
set.seed(1)
x <- sin(seq(0, 40, length.out = 400)) + rnorm(400, sd = 0.05)
rec <- recurrence_matrix(x, m = 3, tau = 5, rr = 0.1)
rec$rr

}
\references{
Eckmann JP, Kamphorst SO, Ruelle D. Recurrence plots of dynamical
systems. Europhys Lett 1987;4(9):973-977.

Marwan N, Romano MC, Thiel M, Kurths J. Recurrence plots for the
analysis of complex systems. Phys Rep 2007;438(5-6):237-329.
}
