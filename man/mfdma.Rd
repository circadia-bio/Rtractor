% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fractal.R
\name{mfdma}
\alias{mfdma}
\title{Multifractal Detrending Moving Average (MFDMA)}
\usage{
mfdma(
  x,
  n_min = 10L,
  n_max = NULL,
  n_scales = 30L,
  theta = 0,
  q = seq(-4, 4, by = 0.1)
)
}
\arguments{
\item{x}{Numeric vector. The time series to analyse.}

\item{n_min, n_max}{Integer. Lower/upper bound of the segment size \code{n}.
Following the reference implementation's guidance: \code{n_min} around
\code{10}; \code{n_max} around 10\% of \code{length(x)}. Defaults: \code{n_min = 10},
\code{n_max = round(length(x) / 10)}.}

\item{n_scales}{Integer. Number of segment sizes to evaluate
(log-spaced between \code{n_min} and \code{n_max}). Default \code{30}.}

\item{theta}{Numeric in \verb{[0, 1]}. Position of the moving-average window:
\code{0} (default, recommended) = backward MFDMA, \code{0.5} = centered,
\code{1} = forward.}

\item{q}{Numeric vector. Multifractal orders to evaluate. Default
\code{seq(-4, 4, by = 0.1)}.}
}
\value{
A list with:
\item{n}{Segment sizes evaluated.}
\item{Fq}{Matrix of the q-th order fluctuation function (segment size
x q).}
\item{tau}{Multifractal scaling exponent tau(q).}
\item{alpha}{Singularity strength alpha(q) (trimmed at both ends by
the local-slope smoothing window; shorter than \code{q}).}
\item{f}{Multifractal spectrum f(alpha).}
\item{q}{The \code{q} values corresponding to \code{alpha}/\code{f} (trimmed to match).}
}
\description{
Estimates the multifractal scaling properties of a time series using
the detrending moving average algorithm (Gu & Zhou 2010). Clean-room
C++ reimplementation from the published algorithm (the reference
MATLAB implementation consulted, \code{MFDMA_1D.m}, had no license header;
see \code{inst/COPYRIGHTS}). The segment-fluctuation core was validated
against a Python transliteration of that reference on synthetic test
data (exact match to displayed precision).
}
\examples{
set.seed(1)
x <- rnorm(4000)
res <- mfdma(x, n_min = 10, n_max = 400, n_scales = 20)
plot(res$alpha, res$f, type = "b")

}
\references{
Gu GF, Zhou WX. Detrending moving average algorithm for multifractals.
Phys Rev E 2010;82:011136.
}
