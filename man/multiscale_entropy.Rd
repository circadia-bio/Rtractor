% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiscale.R
\name{multiscale_entropy}
\alias{multiscale_entropy}
\title{Multiscale Entropy (MSE)}
\usage{
multiscale_entropy(x, scale_max = 20L, m = 2L, r = 0.15)
}
\arguments{
\item{x}{Numeric vector. The time series to analyse.}

\item{scale_max}{Integer >= 1. Largest scale factor to evaluate; MSE is
computed at every integer scale from \code{1} to \code{scale_max}. Default \code{20}
(the standard MSE convention).}

\item{m}{Integer >= 1. Template length, passed to \code{\link[=sample_entropy]{sample_entropy()}}.
Default \code{2} (the standard MSE convention).}

\item{r}{Numeric > 0. Tolerance as a fraction of the \emph{original} series'
standard deviation, passed to \code{\link[=sample_entropy]{sample_entropy()}}. Default \code{0.15} (the
standard MSE convention).}
}
\value{
A list with:
\item{scale}{The scale factors evaluated, \code{1:scale_max}.}
\item{mse}{Sample entropy at each scale (may contain \code{NA} at large
scales, where the coarse-grained series becomes too short to
estimate reliably).}
\item{m, r}{The parameters used, echoed back for reference.}
}
\description{
Estimates the complexity of a time series across a range of temporal
scales (Costa, Goldberger & Peng 2002): the series is coarse-grained
(non-overlapping block-averaged) at each scale factor, and
\code{\link[=sample_entropy]{sample_entropy()}} is computed on each coarse-grained series, using a
tolerance held \emph{fixed} relative to the original (not the coarse-grained)
series' standard deviation -- this is the standard MSE convention and
essential for entropy values to be comparable across scales. Direct
C++ port of the coarse-graining + sample-entropy core in PhysioNet's
reference \code{mse.c} (Costa), validated to reproduce the compiled
reference binary's output exactly (to its own displayed precision) on
synthetic test data. See \code{inst/COPYRIGHTS}.
}
\examples{
set.seed(1)
res <- multiscale_entropy(rnorm(2000), scale_max = 10)
plot(res$scale, res$mse, type = "b")

}
\references{
Costa M, Goldberger AL, Peng CK. Multiscale entropy analysis of complex
physiologic time series. Phys Rev Lett 2002;89:068102.
}
