% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rqa.R
\name{recurrence_microstate_entropy}
\alias{recurrence_microstate_entropy}
\title{Recurrence Microstates Entropy (maximum-entropy threshold search)}
\usage{
recurrence_microstate_entropy(
  x,
  block = 3L,
  n_samples = 18000L,
  eps_min = 1e-09,
  eps_max = 0.499999999,
  frac = 10L,
  frac2 = 10L,
  seed = NULL
)
}
\arguments{
\item{x}{Numeric vector. The (already windowed) time series segment to
analyse. In the original study, \code{x} was min-max normalised to \verb{[0, 1]}
per window before calling this function — do the same if you want
comparable \code{eps_max} values across windows/subjects.}

\item{block}{Integer. Side length of the square recurrence microstate
block; the microstate space has \code{2^(block^2)} possible patterns.
Default \code{3} (matching the reference implementation).}

\item{n_samples}{Integer. Number of random point-pairs sampled to
estimate the recurrence microstate distribution. Default \code{18000}.}

\item{eps_min, eps_max}{Numeric. Search range for the vicinity threshold.
Defaults match the reference implementation.}

\item{frac, frac2}{Integer. Number of steps in the coarse search pass and
number of refinement iterations, respectively. Defaults match the
reference implementation.}

\item{seed}{Optional integer. Seeds the random pair sampling for
reproducibility. The original Julia script reseeds from system entropy
on every run (\verb{Random.seed!()} with no argument) and is therefore not
reproducible run-to-run by design; pass a \code{seed} here if you need
reproducible results, or leave \code{NULL} to match the original's
fresh-randomness-every-call behaviour.}
}
\value{
A list with:
\item{microstate_probs}{Probability of each of the \code{2^(block^2)}
microstates at the entropy-maximising threshold.}
\item{entropy_max}{The maximum Shannon entropy found (\code{S_max}).}
\item{eps_max}{The vicinity threshold at which \code{entropy_max} was
achieved.}
}
\description{
Estimates the complexity of a (windowed) time series using the entropy
of recurrence microstates (Corso et al. 2018), searching over the
vicinity threshold \code{epsilon} to find the value that \emph{maximises} the
Shannon entropy of the microstate distribution — a parameter-free
alternative to fixing \code{epsilon} by hand, related to the optimal-threshold
method of Prado et al. (2018).
}
\details{
Ported from \code{circadia-bio/maxEntropy} (\code{Entropy.jl}, MIT licensed), the
script used to produce the entropy estimates in Ferre et al. (2024,
PLOS ONE). The deterministic core loop was validated against a Python
transliteration of the original on synthetic test data (exact match on
\code{S_max} and \code{eps_max} given identical sampled index pairs). Only the
reusable \code{Max_Entropy()} routine was ported — the original script's
\code{main()} (file I/O over named subject directories) is study-specific
scaffolding, not part of the general-purpose method.
}
\examples{
set.seed(1)
x <- (sin(seq(0, 20 * pi, length.out = 300)) + 1) / 2
recurrence_microstate_entropy(x, seed = 1)

}
\references{
Corso G, Prado TL, dos Santos Lima GZ, Kurths J, Lopes SR. Quantifying
entropy using recurrence matrix microstates. Chaos 2018;28(8):083108.

Prado TL, dos Santos Lima GZ, Lobao-Soares B, do Nascimento GC, Corso G,
Fontenele-Araujo J, Kurths J, Lopes SR. Optimizing the detection of
nonstationary signals by using recurrence analysis. Chaos
2018;28(8):085703.

Ferre IBS, Corso G, dos Santos Lima GZ, Lopes SR, Leocadio-Miguel MA,
Franca LGS, de Lima Prado T, Araujo JF. Cycling reduces the entropy of
neuronal activity in the human adult cortex. PLOS ONE 2024;19(10):e0298703.
}
