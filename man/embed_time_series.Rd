% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed.R
\name{embed_time_series}
\alias{embed_time_series}
\title{Time-Delay (Takens) Embedding}
\usage{
embed_time_series(x, m = 2L, tau = 1L)
}
\arguments{
\item{x}{Numeric vector. The time series to embed.}

\item{m}{Integer >= 1. Embedding dimension.}

\item{tau}{Integer >= 1. Time delay (in samples).}
}
\value{
A numeric matrix with \code{length(x) - (m - 1) * tau} rows and \code{m}
columns; row \code{i} is \verb{(x[i], x[i + tau], ..., x[i + (m - 1) * tau])}.
}
\description{
Reconstructs a phase-space trajectory from a scalar time series using
Takens' (1981) delay-coordinate method.
}
\examples{
embed_time_series(sin(seq(0, 20, length.out = 200)), m = 3, tau = 5)

}
\references{
Takens F. Detecting strange attractors in turbulence. In: Dynamical
Systems and Turbulence, Warwick 1980. Lecture Notes in Mathematics,
vol 898. Springer; 1981:366-381.
}
