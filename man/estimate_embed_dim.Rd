% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed.R
\name{estimate_embed_dim}
\alias{estimate_embed_dim}
\title{Estimate Embedding Dimension via False Nearest Neighbours}
\usage{
estimate_embed_dim(
  x,
  tau = 1L,
  m_max = 10L,
  rtol = 15,
  atol = 2,
  theiler = 0L,
  fnn_threshold = 0.01
)
}
\arguments{
\item{x}{Numeric vector. The time series to analyse.}

\item{tau}{Integer. Time delay, e.g. from \code{\link[=estimate_delay]{estimate_delay()}}. Default \code{1}.}

\item{m_max}{Integer. Largest embedding dimension to test. Default \code{10}.}

\item{rtol}{Numeric. Threshold for the FNN distance-ratio criterion.
Default \code{15}, as in the original paper.}

\item{atol}{Numeric. Threshold for the FNN attractor-size criterion.
Default \code{2}, as in the original paper.}

\item{theiler}{Integer. Temporal exclusion window: candidate neighbours
within \code{theiler} samples of a point in time are ignored, to avoid
picking a temporally- rather than dynamically-close neighbour. Default
\code{0} (no exclusion).}

\item{fnn_threshold}{Numeric in \verb{[0, 1]}. \code{m} is chosen at the first
dimension whose FNN fraction is at or below this value. Default \code{0.01}.}
}
\value{
A list with:
\item{m}{The estimated embedding dimension (\code{NA} if the FNN fraction
never drops to \code{fnn_threshold} within \code{m_max}).}
\item{dim}{The dimensions tested, \code{1:m_max}.}
\item{fnn_fraction}{FNN fraction at each tested dimension.}
}
\description{
Estimates a suitable embedding dimension \code{m} for phase-space
reconstruction using Kennel, Brown & Abarbanel's (1992) false-nearest-
neighbours (FNN) method: for each candidate dimension, the fraction of
points whose nearest neighbour turns out not to be a true dynamical
neighbour (rather than an artefact of projecting a higher-dimensional
attractor down too far) is computed; \code{m} is chosen where this fraction
first drops below \code{fnn_threshold}.
}
\details{
This search is O(N^2) per candidate dimension (brute-force nearest
neighbour), so a warning is issued above 5000 points.
}
\examples{
set.seed(1)
x <- sin(seq(0, 40, length.out = 800)) + rnorm(800, sd = 0.02)
estimate_embed_dim(x, m_max = 6)

}
\references{
Kennel MB, Brown R, Abarbanel HDI. Determining embedding dimension for
phase-space reconstruction using a geometrical construction. Phys Rev A
1992;45:3403-3411.
}
