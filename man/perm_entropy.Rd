% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/entropy.R
\name{perm_entropy}
\alias{perm_entropy}
\title{Permutation Entropy}
\usage{
perm_entropy(x, order = 3L, delay = 1L, normalize = TRUE)
}
\arguments{
\item{x}{Numeric vector. The time series to analyse.}

\item{order}{Integer >= 2. Length of the ordinal pattern (embedding
dimension). Default \code{3}.}

\item{delay}{Integer >= 1. Time delay between pattern elements.
Default \code{1}.}

\item{normalize}{Logical. If \code{TRUE} (default), divide by \verb{log(order!)}
so the result falls in \verb{[0, 1]}. If \code{FALSE}, return the raw Shannon
entropy in nats (natural log). Note this differs from the \code{antropy}
Python library, which computes the raw value in bits (log base 2);
the normalized value is base-independent and matches \code{antropy}
exactly, but the raw values are not directly comparable between the
two. See \code{inst/COPYRIGHTS}.}
}
\value{
A length-1 numeric: the permutation entropy.
}
\description{
Estimates the complexity of a time series using permutation entropy
(Bandt & Pompe 2002): the Shannon entropy of the distribution of
ordinal patterns ("motifs") of length \code{order} found in the series.
Ported from Lucas Franca's own \code{mrpheus} package (part of its AASM
sleep-staging feature pipeline), itself validated for exact parity
against the \code{antropy} Python library; re-validated here directly
against \code{antropy} 0.2.2 on synthetic test data (exact match to
displayed precision). See \code{inst/COPYRIGHTS}.
}
\examples{
set.seed(1)
perm_entropy(rnorm(1000))

}
\references{
Bandt C, Pompe B. Permutation entropy: a natural complexity measure for
time series. Phys Rev Lett 2002;88:174102.
}
