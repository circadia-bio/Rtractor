% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/entropy.R
\name{sample_entropy}
\alias{sample_entropy}
\title{Sample Entropy (SampEn)}
\usage{
sample_entropy(x, m = 2L, r = 0.15)
}
\arguments{
\item{x}{Numeric vector. The time series to analyse.}

\item{m}{Integer >= 1. Template length. Default \code{2} (the standard MSE
convention, per Costa et al.).}

\item{r}{Numeric > 0. Tolerance, as a fraction of \code{x}'s own standard
deviation (i.e. the actual Chebyshev-distance tolerance used is
\code{sd(x) * r}). Default \code{0.15} (the standard MSE convention).}
}
\value{
A length-1 numeric: the sample entropy. If there are too few
valid template pairs to compare (\code{length(x) - m < 2}), returns \code{NA}.
If there are zero matches at either template length (undefined ratio),
returns the conventional fallback \code{-log(1 / (N*(N-1)))} used by the
reference implementation, where \code{N = length(x) - m}.
}
\description{
Estimates the complexity of a time series using sample entropy
(Richman & Moorman 2000): the negative log ratio of template matches
of length \code{m + 1} to template matches of length \code{m}, using a fixed
Chebyshev-distance tolerance. Direct C++ port of the counting core in
PhysioNet's reference \code{mse.c} (Costa), validated to reproduce the
compiled reference binary's output exactly (to its own displayed
precision) on synthetic test data. See \code{inst/COPYRIGHTS}.
}
\details{
This is also the building block \code{multiscale_entropy()} applies at each
coarse-grained scale -- see \code{R/multiscale.R}.
}
\examples{
set.seed(1)
sample_entropy(rnorm(1000))

}
\references{
Richman JS, Moorman JR. Physiological time-series analysis using
approximate entropy and sample entropy. Am J Physiol Heart Circ
Physiol 2000;278(6):H2039-H2049.
}
