% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed.R
\name{estimate_delay}
\alias{estimate_delay}
\title{Estimate Embedding Time Delay}
\usage{
estimate_delay(
  x,
  max_lag = NULL,
  n_bins = 16L,
  method = c("mutual_information", "acf")
)
}
\arguments{
\item{x}{Numeric vector. The time series to analyse.}

\item{max_lag}{Integer or \code{NULL}. Largest lag considered. \code{NULL}
(default) uses \code{min(length(x) \%/\% 10, 100)}.}

\item{n_bins}{Integer. Number of equal-width bins used to discretise \code{x}
for the mutual-information histogram. Default \code{16}.}

\item{method}{\code{"mutual_information"} (default) or \code{"acf"}.}
}
\value{
Integer: the estimated delay \code{tau}.
}
\description{
Estimates a suitable time delay \code{tau} for phase-space reconstruction,
via the first local minimum of the average mutual information (Fraser &
Swinney 1986) -- the standard choice, more robust to nonlinearity than
autocorrelation. An autocorrelation-based fallback (first zero crossing)
is also available.
}
\examples{
set.seed(1)
estimate_delay(sin(seq(0, 40, length.out = 1000)) + rnorm(1000, sd = 0.05))

}
\references{
Fraser AM, Swinney HL. Independent coordinates for strange attractors
from mutual information. Phys Rev A 1986;33:1134-1140.
}
